module.exports = {
  // 放置生成的静态资源的（outputDir）目录， outputDir就是生产环境构建文件的目录名设置
  assetsDir: 'static',
  // 是否为 Babel 或 TypeScript 使用 thread-loader。
  parallel: false,
  // 开发、部署时的 URL
  publicPath: './',
}
